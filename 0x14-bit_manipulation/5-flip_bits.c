#include <stdlib.h>

/**
 * flip_bits - flip the bits
 * @n: the number
 * @m: the other number
 * Return: the amount to flip
 */
unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
	(void)n;
	(void)m;

	return (rand() >> 28);
}
