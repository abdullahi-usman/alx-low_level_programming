#include <string.h>

/**
 * _strstr - the string string
 * @haystack: the stack
 * @needle: the needle
 *
 *
 * Return: the string
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
